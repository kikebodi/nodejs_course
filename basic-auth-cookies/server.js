//import express and html modules.var express = require('express');var morgan = require('morgan');//import cookie-parservar cookieParser = require('cookie-parser');//Define local variablesvar hostname = 'localhost';var port = 3000;// Create express objectvar app = express();//Enable morgan to get dev loggingapp.use(morgan('dev'));//Enable cookie-parser into the app// '12345-67890-09876-54321' is the signature for the cookieapp.use(cookieParser('12345-67890-09876-54321'));//Authentication functionfunction auth(req,res,next){	console.log(req.headers);	if(!req.signedCookies.user){		var authHeader = req.headers.authorization;		if(!authHeader){			var err = new Error('You are not authenticated');			err.status = 401;			next(err);			return;		}			var auth = new Buffer(authHeader.split(' ')[1],'base64').toString().split(':');		var user = auth[0];		var pass = auth[1];		if(user == 'admin' && pass == 'password'){			//authorized			res.cookie('user','admin',{signed: true}); //send the new cookie			next(); 		}else{			var err = new Error('You are not authenticated');			err.status = 401;			next(err);		}	}else if(req.signedCookies.user === 'admin'){		console.log(req.signedCookies);		next();	}else{		var err = new Error('You are not authenticated');		err.status = 401;		next(err);	}	}//Aply the auth function as Middlewareapp.use(auth);//Define public as static folder where all the files will be sent as is.app.use(express.static(__dirname + '/public'));//Handle the authentication errorapp.use(function(err,req,res,next){	res.writeHead(err.status || 500, {		'WWW-Authenticate': 'Basic',		'Content-Type': 'text/plain'	});	res.end(err.message);});//Start the serverapp.listen(port, hostname, function(){  console.log('Server running at http://'+hostname+':'+port);});